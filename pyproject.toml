[project]
name = "aws-notify-platform"
version = "0.1.0"
requires-python = ">=3.13" 

[tool.ruff]
line-length = 88
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]  # pycodestyle, pyflakes, pycodestyle warnings, isort